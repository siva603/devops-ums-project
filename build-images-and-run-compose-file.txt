docker build \
  -t backend:latest \
  ./backend



docker build \
  --no-cache \
  --build-arg VITE_API_BASE_URL=/api \
  --build-arg VITE_USE_API=true \
  -t frontend:latest \
  ./frontend

docker images



mkdir -p /root/data

# Create all data directories
mkdir -p /root/data/mysql
mkdir -p /root/data/backend/logs
mkdir -p /root/data/frontend/logs
mkdir -p /root/data/splunk




# Step 3 - Start EVERYTHING (one command)
docker-compose up -d

# Step 4 - Verify
docker ps