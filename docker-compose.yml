version: '3.8'

services:

  mysql:
    image: mysql:latest
    container_name: mysql-container
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: user_management_system
    networks:
      - ums-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - /root/data/mysql:/var/lib/mysql

  backend:
    image: backend:latest
    container_name: backend-container
    environment:
      DB_HOST: mysql-container
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - ums-network
    volumes:
      - /root/data/backend/logs:/app/logs
    restart: on-failure

  frontend:
    image: frontend:latest
    container_name: frontend-container
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - ums-network
    volumes:
      - /root/data/frontend/logs:/var/log/nginx
    restart: on-failure

networks:
  ums-network:
    driver: bridge