apiVersion: v1
kind: Service
metadata:
  name: ums-frontend-svc
  namespace: ums
spec:
  selector:
    app: ums-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 32000
  type: LoadBalancer