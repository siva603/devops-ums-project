#================================================================
# MySQL Service - Internal access only (ClusterIP)
#================================================================
apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: spring-app
  labels:
    app: mysql
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  type: ClusterIP
